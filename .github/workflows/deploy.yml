name: Build and Push to Docker Hub

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # خطوة مهمة لمحاكاة الأنظمة المختلفة وتفعيل دعم الـ arm64
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # خطوة ضرورية لإنشاء منصة بناء تدعم الـ Multi-platform
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # البناء والرفع مع تحديد المنصات المطلوبة
      - name: Build and Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/xray-vless-grpc:latest
